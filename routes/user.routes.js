const router = require("express").Router();

// Las rutas /latest y /history se movieron a sensor.routes.js bajo /api/sensors/
// Este archivo queda vacío por ahora pero se mantiene para futuras rutas de usuario

module.exports = router;
